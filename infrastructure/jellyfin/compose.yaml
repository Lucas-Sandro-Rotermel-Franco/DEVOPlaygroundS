services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin

    user: root:root

    ports:
      - 8096:8096/tcp
      - 7359:7359/udp

    volumes:
      - ./config:/config
      - ./cache:/cache
      - ${MEDIA_PATH}:/media

    restart: 'unless-stopped'
